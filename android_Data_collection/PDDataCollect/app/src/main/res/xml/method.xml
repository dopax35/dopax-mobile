<?xml version="1.0" encoding="utf-8"?>
<!--
    Input method descriptor for PDKeyboardService.
    Declares two subtypes so Android's language picker shows both English and Hebrew.
    Language switching within the keyboard itself uses the custom עA / EN key.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:isDefault="false">

    <!-- English subtype -->
    <subtype
        android:label="English (PDCollect)"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:imeSubtypeExtraValue="KeyboardLayoutSet=qwerty,AsciiCapable" />

    <!-- Hebrew subtype -->
    <subtype
        android:label="עברית (PDCollect)"
        android:imeSubtypeLocale="iw_IL"
        android:imeSubtypeMode="keyboard"
        android:imeSubtypeExtraValue="KeyboardLayoutSet=hebrew" />

</input-method>
