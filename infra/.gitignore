# Generated by deploy.sh on first run. Contains the PostgreSQL admin password
# and both JWT signing secrets. Losing JWT_SECRET signs every participant out,
# which guardrail R1 forbids — back it up somewhere the team can reach.
.secrets.env

# Compiled ARM output, if anyone runs `az bicep build` without --stdout.
*.json
!*.bicepparam
